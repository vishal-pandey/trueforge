---
"@truefoundry/trueforge-core": patch
"@truefoundry/trueforge": patch
---

Add `turn_inbound_events` store API for durable tip HITL send-event inbox (insert / list unconsumed / mark consumed), with Postgres and SQLite migrations.
