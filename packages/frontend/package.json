{
  "name": "frontend",
  "version": "0.0.0",
  "description": "TrueForge agent chat frontend. Private app — not published to npm.",
  "author": "TrueFoundry",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "ui:build": "pnpm --filter @truefoundry/trueforge-ui build",
    "predev": "pnpm run ui:build",
    "dev": "trap 'exit 0' INT TERM; vite",
    "dev:no-watch": "pnpm run dev",
    "prebuild": "pnpm run ui:build",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretypecheck": "pnpm run ui:build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm run ui:build",
    "test": "cross-env NODE_OPTIONS=--conditions=trueforge-dev tsx --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@assistant-ui/core": "0.3.20",
    "@assistant-ui/react": "0.15.21",
    "@assistant-ui/store": "0.3.14",
    "@truefoundry/trueforge-assistant-ui-runtime": "workspace:*",
    "@truefoundry/trueforge-sdk": "workspace:*",
    "@truefoundry/trueforge-ui": "workspace:*",
    "monaco-editor": "^0.56.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-router-dom": "^7.18.4",
    "thinking-orbs": "^0.3.1"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^4.7.0",
    "cross-env": "^10.1.0",
    "tailwindcss": "4.3.3",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vite": "^6.3.5",
    "vite-plugin-compression2": "^2.5.3",
    "vite-plugin-monaco-editor-esm": "^2.0.2"
  }
}
